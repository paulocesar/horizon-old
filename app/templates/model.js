var model = Schema({
  // _id :  ObjectId,
  // name: String,
  // access : {type: Number, min: 1, max: 2, default: 1},
  // _model : {type: ObjectId, ref: 'Model'},
});

// model.path('name').required(true);

module.exports.schema = model;
module.exports.methods = {

};